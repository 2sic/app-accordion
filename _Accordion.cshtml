@inherits Custom.Hybrid.Razor12
@using ToSic.Razor.Blade;
@using ToSic.Sxc.Blocks;
@* todo 2mh
- all css classes should start with app-accordion4-...
- 
-
- ...
- once done, check/sync work with BS3 version (but be carefuly syncing bs3-standard css-classe, that could/should be different)
*@
<div class="co-accordion-wrapper" @Edit.Attribute("quick-edit-config", new { modules = new {  enable = true }})>
  @foreach (var accord in AsList(Data)) {
    var initiallyExpanded = accord.InitiallyExpanded ?? false;
    var hasIcon = Text.Has(accord.Icon);

    <div class='co-accordion-item @(initiallyExpanded ? "active" : "")' @Edit.TagToolbar(accord)>
      <div id="hash-@accord.EntityId" data-hash="@accord.EntityId" class='co-accordion-title '>
        <h2 class='@(hasIcon ? "" : "co-no-icon")'>
          @if(hasIcon) {
            <span><i class="co-icon fas @accord.Icon" aria-hidden="true"></i></span>
          }
          @accord.Title
        </h2>
      </div>

      @* prevent adding content if still showing the demo-item - this is done by only adding the class as needed *@
      @if(accord.IsDemoItem) {
        <div class='co-accordion-content'>
          @Html.Raw(Resources.DemoItemMessage)
        </div>
      } else {
        <div class='co-accordion-content sc-content-block-list @(initiallyExpanded ? "d-block" : "")' @Edit.ContextAttributes(accord, field: "DesignedContent")>
          @foreach (var designedContent in accord.DesignedContent) {
            @Render.One(designedContent);
          }
        </div>
      }

    </div>
  }
</div>

@* todo: @2mh use page.Activate for font-awesome *@
<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.0.11/css/all.css" integrity="sha384-p2jx59pefphTFIpeqCcISO9MdVfIm4pNnsL08A6v5vaQc4owkQqxMV8kg4Yvhaw/" crossorigin="anonymous">

<link rel="stylesheet" href="@App.Path/dist/bs4.min.css" data-enableoptimizations="true" />
<script src="@App.Path/dist/lib/slideToggle.min.js" data-enableoptimizations="true"></script>
<script src="@App.Path/dist/scripts.min.js" data-enableoptimizations="true"></script>